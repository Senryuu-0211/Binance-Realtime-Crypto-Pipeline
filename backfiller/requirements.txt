# Pinned (no `latest`) so dev and server build byte-identical images.

# HTTP client for the Binance REST historicalTrades API.
requests==2.32.3

# Same official ClickHouse client the consumer/seeder use (column-oriented insert).
clickhouse-connect==0.7.19
